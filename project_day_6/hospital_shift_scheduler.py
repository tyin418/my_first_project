# hospital_shift_scheduler.py

nurses = ["Alice", "Bob", "Charlie"]  # List of nurses
shifts = ["Morning", "Afternoon", "Night"]  # Shift options

# Assign each nurse to a shift
for i in range(len(nurses)):
    print(f"Nurse {nurses[i]} assigned to {shifts[i]} shift.")
